import { expect } from 'chai';
import { describe } from 'mocha';

describe('testes de Book' , () => {
  describe('Deveria instanciar um novo Book' , () => {
    it('instancia correntamente um novo Book?', () =>{
      expect(0).equal(0);
    })
    // it('verificar os atributos do novo Book?', () =>{
    //   expect(model).to.haveOwnProperty('');
    // })
  })
})